<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top" style="margin-bottom: 1rem">
    <a class="navbar-brand" href="{{ path('home') }}">Pizzera La Fattoria</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown active">
                <a class="nav-link dropdown-toggle" href="{{ path('speisekarte') }}" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Speisekarten
                </a>
            </li>
            <li class="navbar-nav mr-auto">
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('speisekarte') }}">Alles</a>
                    {% for group in dishGroups %}
                        <a class="dropdown-item" href="speisekarte#{{ group.name }}">{{ group.name }}</a>
                    {% endfor %}
                </div>
            </li>
        </ul>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item ml-auto">
                <a class="nav-link" href="{{ path('datenschutz') }}">Datenschutz</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('impressum') }}">Impressum</a>
            </li>
        </ul>
    </div>
</nav>